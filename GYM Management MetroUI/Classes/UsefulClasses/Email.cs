using System.Net.Mail;

namespace ClubManagement.Classes.UsefulClasses
{
    /// <summary>
    /// This Email class for mange all sending emails and stuff
    /// </summary>
    class Email
    {
        private string senderEmail, receiverEmail, messageSubject, messageBody;
        public  Email(string senderEmail, string receiverEmail, string messageSubject, string messageBody){
           // this.senderEmail = Properties.Settings.   we will need to set admin email from settings db here

           // MailMessage mail = new MailMessage();
            // SmtpClient client = new SmtpClient();
            // etc...
        }
        

       
    }
}
